front50:
  enabled: false

igor:
  enabled: false

bakery:
  enabled: false

echo:
  enabled: false

monitor:
  activeExecutions:
    redis: true

executionRepository:
  redis:
    enabled: true

spring:
  application:
    name: orca

spinnaker:
  extensibility:
    plugins-root-path: build/plugins
    plugins:
      Armory.ExtensionUsingPluginSdk:
        enabled: true

services:
  fiat:
    baseUrl: https://fiat.net

clouddriver:
  baseUrl: https://clouddriver.net
  readOnly:
    baseUrl: https://clouddriver-readonly.net
